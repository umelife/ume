# ==============================================================================
# RECLAIM MVP - Environment Variables Template
# ==============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# Supabase Configuration (REQUIRED)
# ------------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Public URL - safe to expose to browser
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anonymous key - safe to expose to browser (has RLS protection)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service role key - ⚠️  SERVER-SIDE ONLY! NEVER expose to browser!
# Used for admin operations that bypass RLS
# Only use in API routes, server components, and server actions
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------

# Base URL of your application
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Stripe Payment Configuration (CURRENTLY DISABLED)
# ------------------------------------------------------------------------------
# ⚠️  STRIPE INTEGRATION TEMPORARILY DISABLED UNTIL BUSINESS REGISTRATION
# Leave these empty for now. Fill in only when ready to enable payments.
# Get these from: https://dashboard.stripe.com/test/apikeys

# Stripe publishable key (test mode) - safe to expose to browser
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Stripe secret key (test mode) - ⚠️  SERVER-SIDE ONLY!
# STRIPE_SECRET_KEY=sk_test_your_key_here

# Stripe webhook secret - get from Stripe webhook settings
# Local testing: use `stripe listen` CLI to get whsec_... secret
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ------------------------------------------------------------------------------
# Feature Flags
# ------------------------------------------------------------------------------

# Enable/disable payment processing
# Set to 'false' until LLC is formed and Stripe account is fully activated
# Set to 'true' only when ready to accept real payments
FEATURE_PAYMENTS_ENABLED=false

# Enable/disable admin features
FEATURE_ADMIN_ENABLED=true

# ------------------------------------------------------------------------------
# Email Configuration (Brevo)
# ------------------------------------------------------------------------------
# Get API key from: https://app.brevo.com/settings/keys/api

# Brevo API key for transactional emails
# Leave empty to disable email notifications (emails will be logged only)
BREVO_API_KEY=xkeysib-your-api-key-here

# Brevo verified sender email (FROM address)
# IMPORTANT: Must be verified in Brevo Dashboard -> Senders & IP -> Senders
# Defaults to no-reply@ume-life.com if not set
BREVO_SENDER_EMAIL=no-reply@ume-life.com

# Support email - receives report notifications and used as reply-to
# This is where emails will be delivered (Gmail inbox)
SUPPORT_EMAIL=umelife.official@gmail.com

# ------------------------------------------------------------------------------
# Analytics (Optional)
# ------------------------------------------------------------------------------

# Mixpanel project token - get from https://mixpanel.com/project/YOUR_PROJECT/settings
NEXT_PUBLIC_MIXPANEL_TOKEN=

# ------------------------------------------------------------------------------
# SMS/Notifications (Optional - Future)
# ------------------------------------------------------------------------------

# Twilio credentials (not implemented yet, reserved for future)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=

# ------------------------------------------------------------------------------
# Admin Configuration
# ------------------------------------------------------------------------------

# Comma-separated list of admin user emails
# These users will have access to /admin dashboard
ADMIN_EMAILS=admin@youruniversity.edu

# ==============================================================================
# Security Notes:
# ==============================================================================
# 1. Never commit .env.local - it's in .gitignore
# 2. Never expose service role keys to the browser
# 3. Use environment variables in Vercel for production deploys
# 4. Rotate keys if accidentally exposed
# 5. Use test mode for Stripe until ready for production
# ==============================================================================
